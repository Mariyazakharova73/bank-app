export const SLIDER_DATA: Record<string, { cardGap: number; visibleCards: number; cardPartWidth: number }> = {
  1439: { cardGap: 80, visibleCards: 3, cardPartWidth: 100 },
  1300: { cardGap: 40, visibleCards: 3, cardPartWidth: 40 },
  920: { cardGap: 40, visibleCards: 2, cardPartWidth: 40 },
  500: { cardGap: 40, visibleCards: 1, cardPartWidth: 40 },
  0: { cardGap: 2, visibleCards: 1, cardPartWidth: 0 },
};

export const HTML_TAG_REGEXP = /<[a-z][\s\S]*>/i;
export const CARD_WIDTH = 320;
